# =============================================================================
# Brewfile.work — reserved for a dedicated work machine
# Applied ONLY with --work profile (bash bootstrap.sh --work).
#
# Currently empty — work apps live in Brewfile.personal since they're used
# on the personal Mac. Add machine-specific tools here if a separate work
# machine is ever set up.
# =============================================================================
