# Repo-level ignores — files that must never be committed
.zshrc.local           # machine-specific shell overrides
*.bak.*                # backup files created by bootstrap.sh
Brewfile.lock.json     # generated by brew bundle — never commit
